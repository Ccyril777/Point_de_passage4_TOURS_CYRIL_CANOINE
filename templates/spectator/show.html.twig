{% extends 'base.html.twig' %}

{% block title %}Spectator{% endblock %}

{% block body %}
    <h1>Spectator</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ spectator.id }}</td>
            </tr>
            <tr>
                <th>First_name</th>
                <td>{{ spectator.firstName }}</td>
            </tr>
            <tr>
                <th>Last_name</th>
                <td>{{ spectator.lastName }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ spectator.email }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ spectator.password }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ spectator.role }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ spectator.address }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('spectator_index') }}">back to list</a>

    <a href="{{ path('spectator_edit', {'id': spectator.id}) }}">edit</a>

    {{ include('spectator/_delete_form.html.twig') }}
{% endblock %}
